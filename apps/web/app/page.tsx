'use client'

import styled from 'styled-components'

const StyledPage = styled.div`
  height: 100vh;
  display: grid;
  place-items: center;
  background: black;
  color: #fbfbfb;
`

export default function Index() {
  return <StyledPage>Work in progress</StyledPage>
}
